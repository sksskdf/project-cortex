# GitHub 통합 (Phase 3)
# Personal Access Token — repo + pull-request 권한 필요.
# 조직 레포는 GitHub App 권장 (Phase 8에서 전환 검토).
GITHUB_TOKEN=

# Webhook 검증용 시크릿 — GitHub repo 설정 → Webhooks → Secret과 동일하게.
GITHUB_WEBHOOK_SECRET=

# DB 경로 (옵션). 미설정 시 data/cortex.sqlite.
CORTEX_DB_PATH=
